# @NAME@ - Neon Coffee PlayStation 2 visual novel
#
# Do not run make directly; use `ncc build`. It sets PS2DEV, PS2SDK and GSKIT
# and puts the toolchain on PATH.
#
# The two _data.c files are generated art, made by tools/png2ps2.py. The PS2
# side has no runtime that loads assets yet, so the pixels are compiled in --
# exactly what PS2SDK's own samples do. That is M6.

EE_BIN  = game.elf
EE_OBJS = src/main.o src/logo_data.o src/ui_skin_data.o src/font_data.o src/nc_materials.o src/audio/audio_data.o

EE_LIBS = -ldraw -lgraph -lmath3d -lpacket -ldma -lpad -laudsrv -lpatches -lm

all: $(EE_BIN)

clean:
	rm -f $(EE_BIN) $(EE_OBJS)

include $(PS2SDK)/samples/Makefile.pref
include $(PS2SDK)/samples/Makefile.eeglobal

# Content is generated by ncc before make.
src/main.o: src/vn_content.h src/vn_kit.h src/vn_runtime.h src/nc_audio.h src/nc_world3d.h src/nc_materials.h src/audio/audio_data.h

src/main.o: src/nc_events.h
